# 统一仓库文本行尾为 LF，消除 Windows/CI 间的 CRLF 噪声。
# text=auto：Git 按内容自动判定文本/二进制；eol=lf：检出与提交均规整为 LF。
* text=auto eol=lf

# 显式二进制（不参与行尾转换，避免 corruption）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.gz binary
*.exe binary
*.dll binary
*.so binary
*.a binary
*.obj binary
